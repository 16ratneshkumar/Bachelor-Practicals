\documentclass[11pt,a4paper]{article}
\title{Integrals, Sums and Limits}
\author{Dr. Neeraj Kumar Sharma}
\date{}
\begin{document}
\maketitle
\section{Integrals}
Integrals $\int_{a}^{b}x^2 dx$ inside text

The same integral on display:$$\int_{a}^{b} x^2 dx$$ and multiple integrals:
$$
\int\int_{V}\mu(u,v)\hspace{3pt}du\hspace{3pt}dv
$$
$$
\int\int\int_{V}\mu(u,v,w)\hspace{3pt}du\hspace{3pt}dv\hspace{3pt}dw
$$
$$
\oint_{V} f(s)\hspace{3pt}ds
$$
\section{Sums and products}
Sum $ \sum_{n=1}^{\infty} 2^{-n} = 1$ inside text.

The same sum on display:
$$
\sum_{n=1}^{\infty}2^{-n} = 1
$$

Product $\prod_{i=a}^{b}f(i)$ inside text.

The same product on display:
$$\prod_{i=a}^{b}f(i)$$

\section{Limits}
Limit $\lim_{x\to\infty}f(x)$ inside text.

The same limit on display:
$$\lim_{x\to\infty}f(x)$$
\end{document}